#include <vector>

using namespace std;

class Solution
{
public:
    vector<int> getChildren(int node, vector<vector<int>> graph)
    {
        return graph[node];
    }

    int shortestPathLength(vector<vector<int>> &graph)
    {
    }
};